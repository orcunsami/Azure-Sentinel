| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History** |
|-------------|--------------------------------|--------------------|
| 3.0.2       | 23-08-2026                     | Import now processes each alarm page as it is read and fails the run when a page is missed. The synced tag is gated on a successful SOCRadar write. Corrected the severity casing and alarm URL in the critical alarm rule. Writing the incident severity back to SOCRadar is now opt-in via SyncSeverity and off by default, because Microsoft Sentinel has no Critical severity and the write-back downgraded CRITICAL alarms. Added optional IoC extraction and bookmark-relation enrichment for new incidents, with a retry on transient relation failures. All Azure Resource Manager and Log Analytics endpoints are now resolved from template variables instead of hardcoded URIs. |
| 3.0.1       | 17-06-2026                     | Version bump to align the published package with the catalog; no template or playbook logic changes. |
| 3.0.0       | 26-02-2026                     | Initial release.   |
